# Streamlit Secrets Configuration Example
# 
# For Streamlit Cloud deployment:
# 1. Go to your app settings on share.streamlit.io
# 2. Navigate to "Secrets" section
# 3. Copy and paste this content
# 4. Replace placeholder values with your actual API keys
#
# For local development:
# 1. Copy this file to .streamlit/secrets.toml
# 2. Replace placeholder values
# 3. Never commit secrets.toml to git (already in .gitignore)

# OpenAI API Key (Required for AI analysis and PDF extraction)
OPENAI_API_KEY = "sk-your-openai-api-key-here"

# Optional: Anthropic API Key (alternative LLM)
# ANTHROPIC_API_KEY = "your-anthropic-api-key-here"

# Optional: Financial data API keys (for online mode)
# ALPHA_VANTAGE_API_KEY = "your-alpha-vantage-key-here"
# FMP_API_KEY = "your-financial-modeling-prep-key-here"

# Note: Streamlit automatically exposes secrets as st.secrets["KEY_NAME"]
# The app.py can access these as: st.secrets["OPENAI_API_KEY"]
